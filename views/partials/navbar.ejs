<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
  <div class="container-fluid">

    <div class="d-flex align-items-center w-100">

    
      <!-- LEFT GROUP -->
      <div class="d-flex align-items-center gap-4">
        <a class="navbar-brand" href="/">Bill Manager</a>
        <% if (locals.user) { %>
        <a href="/bill/add" class="text-white text-decoration-none fw-semibold">
          Add Bill
        </a>

        <a href="/bill/summary" class="text-white text-decoration-none fw-semibold">
          Summary
        </a>
      </div>

      <!-- RIGHT GROUP -->
      
      <div class="ms-auto d-flex align-items-center text-white">
        <span class="me-3">Welcome back, <%= user.fullname %></span>
        <a href="/user/logout" class="btn btn-danger btn-sm">Logout</a>
      </div>
    <% } %>

    </div>

  </div>
</nav>

<% if (locals.error) { %>
 <div class="alert alert-danger" role="alert">
  <%= locals.error %>
</div>
<% } %>